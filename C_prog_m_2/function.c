#include <stdio.h>
int check(int); // declare fun
main()
{
  int a;
  printf("enter a value:");
  scanf("%d", &a);
  check(a); // calling fun
}
10 int check(int a) // define fun

    int fact = 1;

while (a >= 1)
{

  printf("\n\t%d x %d %d", a, fact, a fact);

  fact = a;
  a--;
}
return 0;
